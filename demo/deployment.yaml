apiVersion: v1
kind: Service
metadata:
  name: adc-sv
  labels:
    app: adc-sv
spec:
  selector:
    app: adc-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  type: LoadBalancer
        
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adc-dp
  labels:
    app: adc-dp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: adc-app
  template:
    metadata:
      name: adc-app-pod
      labels:
        app: adc-app
    spec:
      containers:
      - name: adc-app
        image: docker.io/parkkihong/silvergold799:v1.2
        imagePullPolicy: Always
        ports:
        - containerPort: 8080